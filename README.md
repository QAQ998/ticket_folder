# 私人电子票夹

一个给影迷保存实体电影票根的原生 iOS App。它的目标不是做通用影片清单，而是把每一张实体票根一张一张录入、保存和回看，形成一个私人的电子票夹。

## v1 功能

- 拍摄或从相册选择电影票根图片
- 使用 Apple Vision 在设备端 OCR 识别票面文字
- 手动修正电影名、观影时间、影院、影厅、座位、票价
- 使用 TMDB 搜索并补全海报、导演、年份等电影资料
- 本机保存票根图片、观影记录和海报缓存
- 票夹式首页、详情页、搜索、编辑和删除

## 不在 v1 范围

- 豆瓣主页或影评同步
- 账号系统和云同步
- 手动上传电影海报
- 服务器端 OCR
- 社交分享或公开主页

## TMDB API Key

当前 `Info.plist` 里的 `TMDB_API_KEY` 默认留空。留空时 App 仍可手动保存电影资料；填入 TMDB API key 后，电影搜索和海报缓存能力会启用。

后续建议把 key 改为通过本地 `Local.xcconfig` 或后端代理注入，避免把真实密钥提交到 GitHub。

## 技术栈

- SwiftUI
- SwiftData
- Vision
- PhotosUI
- TMDB API
