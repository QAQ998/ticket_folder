# 散场记技术方案

## 1. 技术目标

散场记 v1 的技术目标是先做出一个本地优先、隐私友好、可持续演进的原生 iOS App。

核心原则：

- 票根图片和 OCR 默认留在设备本地。
- 电影资料源可以替换，客户端不绑定单一平台。
- 资料源失败不影响票根保存。
- 识别和匹配结果都要允许用户确认。
- 为未来上架、iCloud 备份、中文资料源增强预留架构。

## 2. 当前技术栈

- 客户端：SwiftUI
- 数据持久化：SwiftData
- 图片存储：App 沙盒文件系统
- OCR：Apple Vision
- 电影资料：`TMDBClient` 兼容层 + 本地/云端代理
- 本地开发代理：Python `http.server`
- 云函数探索：阿里云 Function Compute
- 最低系统：iOS 17+

## 3. 客户端架构

### 3.1 分层

```text
SwiftUI Views
  |
  |-- TicketRecordEditorView
  |-- TicketWalletHomeView
  |-- TicketSearchView
  |-- TicketRecordDetailView
  |
Services
  |
  |-- TicketOCRService
  |-- TicketParsingService
  |-- TMDBClient / MovieMetadataClient 兼容层
  |-- TicketImageStore
  |-- PosterCacheStore
  |
Models
  |
  |-- MovieRecord
```

### 3.2 视图层

视图层只负责：

- 展示数据。
- 触发用户操作。
- 展示加载、错误和确认状态。
- 调用 Service 完成 OCR、解析、资料获取和图片保存。

视图层不应直接：

- 拼接远程 API 请求。
- 解析 HTML。
- 保存 API Key。
- 执行复杂 OCR 规则。

### 3.3 服务层

服务层负责：

- OCR：从图片得到文本。
- 解析：从 OCR 文本得到结构化候选。
- 资料源：根据片名搜索影片、拉取详情、缓存海报。
- 图片：保存、替换、删除票根图和海报图。

## 4. 数据模型

### 4.1 当前模型

`MovieRecord` 当前包含：

- 基础影片字段：`movieTitle`、`originalTitle`、`year`、`director`
- 外部资料字段：`tmdbId`、`metadataSource`、`metadataFetchedAt`
- 海报缓存字段：`posterLocalPath`、`posterCachedAt`
- 票面字段：`watchedAt`、`cinema`、`hall`、`seat`、`ticketPrice`
- 图片字段：`ticketImagePaths`
- 私人记录：`rating`、`note`
- 时间戳：`createdAt`、`updatedAt`

### 4.2 命名债务

当前 `tmdbId` 和 `TMDBClient` 已经承担了通用电影资料源兼容层的职责。

短期为了减少改动，保留旧命名；后续建议：

- `TMDBClient` -> `MovieMetadataClient`
- `TMDBMovieSearchResult` -> `MovieSearchResult`
- `TMDBMovieMetadata` -> `MovieMetadata`
- `tmdbId` -> `metadataId` 或拆成 `tmdbId`、`doubanId`

### 4.3 建议新增字段

后续资料源增强时建议增加：

- `metadataProvider`：`tmdb` / `doubanPrototype` / `manual` / future provider
- `doubanId`
- `doubanURL`
- `metadataConfidence`
- `metadataRawTitle`
- `metadataUpdatedAt`
- `parsingVersion`

## 5. 图片存储

### 5.1 票根图片

- 保存到 App 沙盒。
- 当前每条记录只使用第一张图片。
- 替换图片时，旧图片进入待删除列表，保存后清理。
- 取消编辑时，清理本次新导入的图片。

### 5.2 海报缓存

- 远程海报下载后保存到本地缓存。
- 记录中只保存本地路径。
- 删除或替换资料时清理旧海报。

### 5.3 后续要求

- 增加图片压缩策略。
- 增加原图和展示图分层。
- 增加图片质量检测。
- 增加敏感信息遮挡能力。

## 6. OCR 技术方案

### 6.1 当前方案

当前使用 Apple Vision 在设备端执行 OCR。

优势：

- 不需要服务器。
- 不上传票根图片。
- 不需要 API Key。
- 成本低。
- App Store 合规风险低。

限制：

- 票根版式差异大，纯规则解析容易误判。
- 低质量热敏纸、折痕、错位、重叠文字会降低准确率。
- Vision 只能识别文本，不理解“哪个字段对应哪个值”。

### 6.2 当前解析策略

```text
票根图片
  -> Apple Vision OCR
  -> 文本行清洗
  -> 字段规则匹配
  -> 平台/影院/噪声过滤
  -> 片名候选队列
  -> 高置信字段自动填入
  -> 低置信字段留给用户确认
```

### 6.3 后续增强方向

优先级从低成本到高成本：

1. 扩充规则库和样本回归。
2. 使用 Vision 文本框坐标做版式分析。
3. 本地 OCR + 文本大模型做结构化解析。
4. 云 OCR 或多模态大模型识别票根。

### 6.4 推荐中期方案

中期建议采用：

```text
Apple Vision OCR
  -> 本地文本清洗
  -> 后端或本地轻量模型结构化
  -> 返回字段 + 置信度 + 候选解释
```

这样比纯云 OCR 更省钱，也比纯规则更能适应复杂票根。

## 7. 电影资料源方案

### 7.1 问题背景

TMDB 的稳定性和授权状态较好，但中文片名体验不够理想：

- 中文译名不完整。
- 港译、台译、内地译名覆盖不足。
- OCR 片名稍有后缀就可能匹配失败。
- 中文影迷更习惯豆瓣条目、豆瓣编号和豆瓣链接。

因此散场记不应把客户端绑定在 TMDB，而应做可替换电影资料源。

### 7.2 推荐架构

```text
iOS App
  |
  |  search/movie, movie/detail, poster
  v
Movie Metadata Proxy
  |
  |-- Provider A: TMDB
  |-- Provider B: Douban Prototype
  |-- Provider C: future authorized Chinese source
  |-- Provider D: local/manual fallback
```

### 7.3 客户端接口原则

客户端只关心统一数据结构：

- id
- title
- originalTitle
- year
- director
- posterURL
- subjectURL
- provider
- confidence

客户端不应该知道：

- 豆瓣页面结构。
- 某个内部接口地址。
- API Key。
- Cookie。
- 代理池。

### 7.4 豆瓣原型方案

当前本地代理已实现豆瓣原型适配，用于验证中文匹配体验。

尝试来源：

- 豆瓣移动端搜索接口。
- 豆瓣搜索页内嵌数据。
- 豆瓣 subject suggest。
- 豆瓣详情页解析。

当前能力：

- 可根据中文片名搜索候选。
- 可返回豆瓣 ID。
- 可返回豆瓣 subject 链接。
- 可返回部分海报、年份。
- 可从搜索副标题中尝试提取导演。

当前风险：

- 匿名访问会出现 `need_login`。
- 搜索页可能返回“访问太频繁”。
- 同一关键词在不同时间可能成功或失败。
- 详情页解析可能被登录或风控拦截。
- 不适合作为正式上架版本唯一资料来源。

### 7.5 豆瓣方案使用边界

允许：

- 本地原型验证。
- 小流量开发测试。
- 评估中文片名匹配质量。
- 设计豆瓣 ID / 豆瓣链接的数据结构。

不建议：

- 在客户端内直接爬豆瓣。
- 使用用户 Cookie。
- 使用代理池绕限制。
- 将豆瓣抓取作为正式商业版本唯一资料源。
- 长期缓存和展示未经授权的豆瓣内容。

### 7.6 正式资料源策略

建议分阶段：

1. 保留 TMDB 作为稳定兜底。
2. 保留豆瓣原型作为中文匹配验证工具。
3. 支持保存豆瓣 ID 和豆瓣链接，用于跳转和识别锚点。
4. 评估授权中文资料源或第三方聚合 API。
5. 后端做资料源聚合、缓存、置信度排序和降级。

## 8. 本地代理

### 8.1 作用

本地代理用于：

- 避免手机直接访问 TMDB 或其他资料源失败。
- 隐藏 API Key。
- 快速切换资料源。
- 验证豆瓣原型。

### 8.2 当前文件

- `server/local-dev-proxy/app.py`

### 8.3 当前环境变量

- `PORT`：默认 8787。
- `SANCHANGJI_METADATA_PROVIDER`：默认 `douban`，可切换资料源策略。
- `SANCHANGJI_PROXY_UPSTREAM`：上游代理地址。

### 8.4 Local.xcconfig

本地开发通过 `Local.xcconfig` 注入：

```text
TMDB_API_BASE_URL = http://<本机局域网 IP>:8787/3
TMDB_IMAGE_BASE_URL = http://<本机局域网 IP>:8787/t/p/w500
TMDB_API_KEY =
```

注意：

- `Local.xcconfig` 不提交 Git。
- 不在仓库内保存真实 API Key。

## 9. 云函数方案

### 9.1 目标

云函数用于未来：

- 保护 API Key。
- 提高手机访问稳定性。
- 做资料源聚合。
- 做缓存和限流。
- 为上架版本提供可控后端入口。

### 9.2 阿里云 FC

已探索阿里云 Function Compute。

适合：

- 国内网络环境。
- 轻量 HTTP 代理。
- 低频搜索请求。

需要注意：

- 默认公网域名不适合长期生产。
- 生产应绑定自定义域名或 API 网关。
- 需要日志、限流、缓存、错误监控。

## 10. 搜索与匹配策略

### 10.1 查询清洗

电影名进入资料源搜索前需要清洗：

- 去除语言版本：英文、中文、国语、原版。
- 去除放映格式：2D、3D、4D、IMAX、CINITY、杜比、中国巨幕。
- 去除末尾括号说明。
- 保留真实片名中的冒号、数字和副标题。
- 过滤影院、平台、发行标识和编码噪声。

明确不进入电影资料搜索队列的例子：

- `China Film`
- `Chinafilm`
- `中国电影`
- `中影`
- `猫眼`
- `淘票票`
- `CGV`

### 10.2 中文核心词回退

当完整片名搜索失败时，可以生成候选查询：

- 原始清洗片名。
- 冒号前主标题。
- `之` 前主标题。
- 前 2 到 3 个中文字符。

示例：

- `哪吒之魔童闹海` -> `哪吒`
- `阿凡达：火与烬` -> `阿凡达`
- `一战再战(英文)` -> `一战再战`

### 10.3 候选排序

排序因子：

- 标题完全匹配。
- 标题包含关系。
- 年份存在。
- 海报存在。
- 资料源可信度。
- OCR 片名置信度。

### 10.4 候选重试

OCR 解析不应只保存一个片名，而应产出片名候选队列。

查询流程：

1. 当前表单片名先进入过滤和清洗。
2. 如果当前表单片名是影院、平台、发行标识或噪声，则跳过。
3. 使用第一个有效候选搜索电影资料。
4. 若无结果，自动尝试下一个候选。
5. 若某个候选成功返回资料，回填年份、导演、海报等影片信息。
6. 所有候选失败后，再提示用户手动修正。

这样可以避免 `China Film` 这类影院品牌误识别为片名后直接停止。

### 10.5 片名写入原则

`movieTitle` 的可信来源只有两类：

- OCR 票面候选。
- 用户手动确认或修改。

电影资料源返回的标准片名、原始片名或外文片名不能在自动流程中直接覆盖 `movieTitle`。资料源只负责补全年份、导演、海报、外部 ID 等元数据；如果当前 `movieTitle` 为空或被判定为平台、影院、编码、发行标识等噪声，只能改写为本次成功匹配的 OCR 票面候选。

资料源返回的标题可以作为 `originalTitle` 或后续 `metadataRawTitle` 保存，用于调试、候选展示和跳转外部条目，但不作为主片名直接展示。

### 10.6 低置信处理

低置信时不应自动覆盖用户字段。

触发条件：

- 多个候选分数接近。
- 第一候选和 OCR 片名差距大。
- 搜索结果只来自核心词回退。
- 年份和票面时间明显冲突。
- 多个 OCR 片名候选均失败。

处理：

- 展示候选列表。
- 用户确认后再写入。

## 11. 错误与降级

### 11.1 OCR 失败

- 不阻断保存。
- 显示温和提示。
- 允许手动输入。

### 11.2 资料源失败

- 不阻断保存。
- 保留用户填写的票面信息。
- 可后续重试。

### 11.3 图片保存失败

- 阻断当前图片导入。
- 提示用户重新选择。

### 11.4 删除失败

- 优先保证记录状态一致。
- 文件清理失败可以后续清理，不影响主流程。

## 12. 安全与隐私

### 12.1 客户端

- 不上传票根图片。
- 不保存用户 Cookie。
- 不在 App 内写死密钥。
- 删除操作二次确认。

### 12.2 服务端

- 不接收票根图片。
- 搜索请求只包含片名关键词。
- 生产环境需要限流。
- 生产环境需要日志脱敏。

## 13. 测试策略

### 13.1 单元测试方向

- 片名清洗。
- 影院过滤。
- 平台过滤。
- 日期解析。
- 票价解析。
- 资料源响应解析。

### 13.2 样本回归

每次新增 OCR 规则，需要用已有真实票根样本回归：

- 猫眼 / 纵横西丽店。
- 星河寰宇城。
- CGV 深圳壹方城。
- 中影国际影城横岗店。

### 13.3 手动测试

- 从相册导入。
- OCR 后编辑。
- 电影资料获取。
- 替换图片。
- 删除图片。
- 保存后重启 App。
- 本地代理开启和关闭两种状态。

## 14. 文档维护规则

每次产品或实现方向更新后，都必须检查并更新：

- `PRD.md`
- `TECHNICAL_DESIGN.md`
- `OCR_RULES.md`

检查重点：

- 功能范围是否变化。
- 用户流程是否变化。
- 数据字段是否变化。
- OCR 规则是否变化。
- 资料源策略是否变化。
- 隐私和合规说明是否变化。
- 验收标准是否变化。
