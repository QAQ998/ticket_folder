# 散场记 PRD

## 1. 文档信息

- 产品名称：散场记
- 产品类型：原生 iOS 私人电子票夹
- 当前版本：v1 原型 / MVP
- 目标平台：iPhone 优先，iOS 17+
- 核心技术栈：SwiftUI、SwiftData、Apple Vision OCR、本地文件存储、可替换电影资料源代理
- 文档状态：开发基线文档

## 2. 产品定位

散场记是一款给影迷保存电影票根的私人电子票夹。

它不是通用影片清单，也不是影评社区，而是帮助用户把每一张实体电影票、电子票截图一张一张录入、保存和回看。票根本身是核心资产，电影资料、海报、评分和短评都是为了辅助这张票根被更好地归档。

用户可以散场后慢慢收好一张票，也可以开场前先把电子票截图存进来。

## 3. 产品目标

### 3.1 v1 目标

- 让用户可以快速保存一张纸质票根或电子票截图。
- 通过 OCR 自动提取票面信息，减少手动输入。
- 通过电影资料源补全年份、导演、海报等基础资料；主片名以票面识别或用户确认为准。
- 建立简洁、克制、有收藏感的私人票夹体验。
- 以本地优先方式保存数据，降低隐私和服务器成本。
- 为未来 App Store 上架、iCloud 备份、中文电影资料源优化预留架构。

### 3.2 非目标

- v1 不做公开主页、社交动态、好友关系。
- v1 不做影评社区或长影评编辑器。
- v1 不做完整票务平台订单管理。
- v1 不做多设备实时同步。
- v1 不做商业级豆瓣数据抓取能力。

## 4. 目标用户

### 4.1 核心用户

- 有保存电影票根习惯的影迷。
- 经常去影院、影展、点映、重映或特别放映的用户。
- 希望把散落在相册里的票根照片整理成私人档案的人。
- 对电影中文译名、港译、台译、影城信息比较敏感的用户。

### 4.2 次级用户

- 想记录观影历史，但不想维护复杂片单的人。
- 希望以后通过 iCloud 备份保留票根数据的人。
- 偶尔保存电子票截图，希望快速检索的人。

## 5. 核心价值

- 票根优先：每条记录围绕一张票根建立，而不是围绕一部电影建立。
- 私人收藏：记录只属于用户本人，默认不公开、不社交。
- 自动辅助：OCR 和电影资料源负责减少输入，但最终由用户确认。
- 长期可留：纸质票会褪色，截图会被相册淹没，散场记把它们整理到一个更稳定的票夹里。
- 中文友好：优先考虑中文片名、内地译名、港译、台译和影院票面习惯。

## 6. v1 功能范围

### 6.1 首页

#### 功能

- 展示已保存的观影记录。
- 以票夹/票根收藏的方式呈现记录。
- 支持进入搜索。
- 支持进入新增票根。
- 底部 Tab 包含：首页、日历、设置。

#### 内容

- 页面标题：散场记。
- 记录卡片信息：
  - 电影名
  - 观影时间
  - 影院
  - 海报或默认占位图
  - 票根图片缩略图或票根视觉元素

#### 空状态

- 没有记录时，引导用户添加第一张纸质票根或电子票根。
- 空状态不应像普通工具 App，应保持“私人票夹”的气质。

### 6.2 新增 / 编辑票根

#### 功能

- 从相册选择一张纸质票根照片或电子票截图。
- 当前版本每条记录只保存一张票根图片。
- 上传后自动执行 OCR。
- OCR 完成后自动预填可识别字段。
- 用户可以修正每个字段。
- 用户可以点击图片预览全图。
- 预览中支持更换图片和删除图片。
- 删除图片需要二次确认。
- 保存记录。

#### 字段

- 票根图片，必填建议，但 v1 可允许无图保存以避免阻断测试。
- 电影名，保存记录时必填。
- 观影时间，默认当前时间，可手动改。
- 影院，非必填。
- 影厅，非必填。
- 座位，非必填。
- 票价，非必填。
- 年份，非必填，由电影资料源补全或用户修正。
- 导演，非必填，由电影资料源补全或用户修正。
- 海报，默认从电影资料源拉取，不支持用户手动上传海报。
- 评分，非必填。
- 短评，非必填。

#### 交互原则

- 不把 OCR 原始结果整段展示给用户。
- 字段旁可以保留候选选择能力，但候选入口要轻，不占据主视觉。
- 默认尽量自动填入高置信字段。
- 不确定时不强行填，避免明显错误伤害信任。
- 用户点击输入框时，需要有明确激活样式。
- 输入框、按钮、选择控件保持统一宽度、圆角和视觉规范。

### 6.3 图片预览

#### 功能

- 点击票根图片进入全图预览。
- 再次点击图片退出预览。
- 预览左上角提供删除照片。
- 预览右上角提供关闭按钮。
- 预览底部提供更换照片。
- 删除需要二次确认。

#### 设计要求

- 删除、关闭和更换照片按钮必须在深色预览背景上清晰可见。
- 不遮挡票根主体信息。
- 图片保持完整展示优先，允许缩放体验后续优化。

### 6.4 OCR 识别

#### 功能

- 使用 Apple Vision 在设备端识别票面文字。
- 自动识别：
  - 电影名
  - 观影时间
  - 影院
  - 影厅
  - 座位
  - 票价
- 过滤明显不是片名的内容：
  - 购票平台
  - 影院品牌和门店
  - 订单号
  - 取票码
  - 二维码说明
  - 服务说明
  - 英文标签

#### 原则

- OCR 是辅助工具，不是最终判断。
- 错误识别不能阻断保存。
- 对片名识别要保守，尤其不能把猫眼、淘票票、大麦等平台名识别成电影名。
- 对影院识别要覆盖品牌、简称、商场门店和地名门店。

### 6.5 电影资料补全

#### 功能

- 根据 OCR 或用户修正后的电影名自动搜索电影资料。
- 返回候选电影列表。
- 高置信时自动选择第一候选并回填。
- 低置信时允许用户选择候选。
- 如果第一片名候选没有匹配到资料，应自动尝试下一个片名候选。
- 明显是影院、平台或发行标识的候选不能进入电影资料搜索队列，例如 `China Film`。
- 回填：
  - 标准片名
  - 年份
  - 导演
  - 海报
  - 外部条目 ID 或链接

#### 资料源策略

v1 使用可替换的电影资料源代理，不把客户端绑定死在单一来源。

- TMDB：作为稳定授权资料源和兜底源。
- 豆瓣原型：用于验证中文片名、港译、台译、豆瓣编号和豆瓣链接的匹配体验。
- 后续可扩展其他中文资料源或授权服务。

#### 重要限制

- 不要求用户粘贴豆瓣链接作为主流程。
- 豆瓣链接或编号可以作为后续高级兜底能力，但不能成为核心体验。
- 豆瓣抓取/内部接口只作为本地原型验证，不应直接作为正式上架版本的唯一数据源。

### 6.6 搜索

#### 功能

- 搜索已保存票根。
- 搜索电影资料候选。
- 支持实时关键词搜索。
- 记录最近搜索关键词，最多 5 个。

#### 搜索范围

- 电影名
- 原始片名
- 导演
- 影院
- 影厅
- 座位
- 短评

### 6.7 日历页

#### v1 内容

- 展示观影日期聚合。
- 当前可作为占位页面，但需要有真实页面结构。
- 后续支持按日、月浏览观影记录。

### 6.8 设置页

#### v1 内容

- 资料源状态。
- 本地存储说明。
- iCloud 备份规划入口或说明。
- 隐私说明。
- App 版本信息。

## 7. 核心用户流程

### 7.1 从相册录入票根

1. 用户进入首页。
2. 用户点击新增票根。
3. 用户点击票根图片区域。
4. 系统打开相册选择器。
5. 用户选择一张票根图片或电子票截图。
6. App 保存图片到沙盒。
7. App 自动执行 OCR。
8. App 自动预填高置信字段。
9. App 根据电影名自动请求电影资料源。
10. App 回填海报、年份、导演，主片名仍以票面候选为准。
11. 用户检查并修正字段。
12. 用户保存记录。
13. 首页出现新记录。

### 7.2 OCR 低置信度流程

1. OCR 完成后，电影名为空或疑似错误。
2. App 不强行保存错误片名。
3. 字段保持空或使用更保守候选。
4. App 先用片名候选队列自动尝试资料源匹配。
5. 所有候选都失败后，用户可从候选入口选择文本，或手动输入。
6. 用户输入片名后，App 再触发电影资料搜索。

### 7.3 电影资料候选选择流程

1. App 用电影名搜索资料源。
2. 如果第一候选高置信，自动回填。
3. 如果多个候选相近，展示候选列表。
4. 用户选择正确影片。
5. App 写入年份、导演、海报；只有当当前片名为空或被判定为噪声时，才写入本次成功匹配的 OCR 票面候选片名。

### 7.4 删除流程

1. 用户在详情页或编辑页触发删除。
2. App 弹出二次确认。
3. 用户确认后删除记录或图片。
4. 本地结构化数据和图片文件同步清理。

## 8. 数据结构

### 8.1 MovieRecord

- id：UUID
- movieTitle：电影名
- originalTitle：原始片名
- year：年份
- director：导演
- tmdbId：当前复用为外部电影资料 ID，后续应重命名为 metadataId 或 externalId
- metadataSource：资料来源，manual / movieMetadata / future provider
- metadataFetchedAt：资料获取时间
- posterLocalPath：海报本地缓存路径
- posterCachedAt：海报缓存时间
- watchedAt：观影时间
- cinema：影院
- hall：影厅
- seat：座位
- ticketPrice：票价
- ticketImagePaths：票根图片路径数组，v1 只使用第一张
- rating：评分，0 表示未评分
- note：短评
- createdAt：创建时间
- updatedAt：更新时间

### 8.2 后续建议字段

- doubanId：豆瓣条目 ID
- doubanURL：豆瓣条目链接
- metadataProvider：资料源名称
- metadataConfidence：匹配置信度
- rawOCRText：原始 OCR 文本，需评估隐私和存储必要性
- parsingVersion：OCR 规则版本
- imageQualityWarning：图片质量提示

## 9. 技术约束

- iOS 17+。
- SwiftUI 构建界面。
- SwiftData 保存结构化数据。
- App 沙盒保存票根图片和海报缓存。
- Apple Vision 在设备端 OCR。
- 电影资料请求通过客户端或代理服务完成。
- API Key 不提交到 GitHub。
- 生产版本应避免使用无授权爬虫作为唯一资料来源。

## 10. 隐私与合规

### 10.1 用户数据

票根可能包含：

- 取票码
- 订单号
- 票号
- 手机号尾号
- 影院位置
- 观影时间
- 支付方式

v1 默认策略：

- 票根图片保存在本机。
- OCR 在设备端完成。
- 不上传票根图片到服务器。
- 电影资料搜索只上传电影名关键词，不上传票根图片。

### 10.2 第三方资料

- TMDB 需要遵守 attribution 和缓存规则。
- 豆瓣资料抓取或内部接口存在稳定性和授权风险。
- 上架前需要明确第三方数据来源、授权边界、隐私政策和 App Store 隐私声明。

## 11. 成功指标

### 11.1 体验指标

- 用户可以在 1 分钟内保存一张票根。
- 用户不需要理解 OCR、TMDB、豆瓣等技术概念。
- 识别失败时，用户仍能顺利保存。
- 删除、替换图片等危险操作都有二次确认。

### 11.2 识别指标

- 常见纸质票根能识别出至少 3 个关键字段。
- 购票平台名不应被填入电影名。
- 影院名不应被填入电影名。
- 带语言和放映格式后缀的片名应自动清洗。

### 11.3 技术指标

- 离线可浏览已保存记录。
- 本地图片和数据可持久化。
- 资料源失败不影响保存票根。
- 搜索请求失败时有明确但不吓人的提示。

## 12. v1 验收标准

- 可以从相册导入一张票根图片。
- 导入后自动 OCR。
- OCR 后可以预填电影名、观影时间、影院、影厅、座位、票价中的部分字段。
- 所有字段都可以被用户修改。
- 电影名末尾的 `(英文)`、`(中文3D)`、`CINITY3D` 等后缀不会污染最终片名。
- `China Film`、`Chinafilm`、`中国电影` 等影院或发行标识不会被识别成电影名。
- 第一片名候选无法匹配电影资料时，会自动尝试后续片名候选。
- 可以通过电影资料源获取海报、年份、导演。
- 电影资料源返回的标准片名不能直接覆盖票面片名，避免出现票根照片里没有的片名。
- 电影资料源失败时仍可保存。
- 首页可以看到已保存记录。
- 详情页可以查看票根图片、影片信息、票面信息和私人记录。
- 点击票根图片可以预览全图。
- 预览页左上角是删除按钮，右上角是高对比关闭按钮，底部只保留更换照片。
- 删除图片、删除记录需要二次确认。
- 搜索页可以搜索本地记录和远程电影资料。
- 日历页和设置页有基础页面结构。

## 13. 版本路线

### v1.0 原型

- 单张票根录入。
- 本地 OCR。
- 本地保存。
- 电影资料源补全。
- 首页、搜索、详情、编辑、日历、设置。

### v1.1 识别增强

- 更完整的 OCR 规则库。
- 低置信度候选选择体验。
- 票根图片质量提示。
- 影院和购票平台词库扩充。

### v1.2 资料源增强

- 中文资料源聚合层。
- 豆瓣 ID 和豆瓣链接关联。
- 资料源置信度评分。
- 资料源失败降级策略。

### v2.0 备份与同步

- iCloud 备份或 CloudKit 同步。
- 多设备恢复。
- 数据导出。

### v2.x 收藏体验

- 多张票根图片。
- 年度回顾。
- 影院、城市、影展维度统计。
- 敏感信息遮挡。
- 更完整的票根相册浏览。
